<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- seekbar 배경 1 -->
    <item android:id="@android:id/background">
        <!-- 선 : 길이 -->
        <shape android:shape="line">
            <stroke android:width="5dp" android:color="@color/barGrayColor" />
        </shape>
    </item>

    <!-- seekbar 프로그래스 바 -->
    <item>
        <clip>
            <shape android:shape="line">
                <stroke android:width="6dp" android:color="@color/textView_KOSPI" />
            </shape>
        </clip>
    </item>
</layer-list>